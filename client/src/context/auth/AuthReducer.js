import {
  ADD_CONTACT,
  DELETE_CONTACT,
  SET_CONTACT,
  CLEAR_CONTACT,
  FILTER_CONTACTS,
  CLEAR_FILTER,
  UPDATE_CONTACT
} from "../types";
const ContactReducer = (state, action) => {
  return null;
};

export default ContactReducer;
